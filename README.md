# game1

Две браузерные мини-игры без сборки и зависимостей:

- `/` — **Nebula Drift** (`index.html`)
- `/pool.html` — **Pool Night**, бильярд на Three.js с PBR-графикой

Локально: `npm start` и открыть http://localhost:3000
